from flask import Blueprint, current_app, request, jsonify

from services.${name}_service import get_all
from forms.${name}_form import ${model_name}_form

form = ${model_name}_form()

${name}_blueprint = Blueprint('${name}', __name__, url_prefix='/api/${name}')

@${name}_blueprint.route('/get_all', methods=['GET'])
def get_all_${name}():
    return jsonify(get_all())